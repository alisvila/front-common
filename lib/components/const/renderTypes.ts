import {ColumnsRenderTypes} from "../rbc-icons/rbc-types/TableType";

const RENDER_TYPES: Record<string, ColumnsRenderTypes> = {
  link: 'link',
  status: 'status',
  tag: 'tag',
}

export default RENDER_TYPES